# Salin ke .env untuk pengujian lokal (jangan commit .env)
DATABASE_URL=postgresql://USER:PASSWORD@HOST/neondb?sslmode=require

# Set ke 1 untuk mulai dari offset 0 (default kosong = lanjutkan offset tersimpan)
# MULAI_DARI_AWAL=1
